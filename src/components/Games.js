import React from 'react';
import MainNavbar from './MainNavbar';

const Games = (props) => {
    return(
        <div>
            <MainNavbar games />
            <h1>Games</h1>
        </div>
    );
}

export default Games;

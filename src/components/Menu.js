import React from 'react';
import MainNavbar from './MainNavbar';

const Menu = (props) => {
    return(
        <div>
            <MainNavbar menu />
            <h1>Menu</h1>
        </div>
    );
}

export default Menu;

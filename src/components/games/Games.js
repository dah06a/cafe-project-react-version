import React from 'react';
import MainNavbar from '../navigation/MainNavbar';

const Games = (props) => {
    return(
        <div>
            <MainNavbar games />
            <h1>Games</h1>
        </div>
    );
}

export default Games;

import React from 'react';
import MainNavbar from '../navigation/MainNavbar';

const About = (props) => {
    return(
        <div>
            <MainNavbar about />
            <h1>About</h1>
        </div>
    );
}

export default About;

import React from 'react';
import MainNavbar from '../navigation/MainNavbar';

const Reserve = (props) => {
    return(
        <div>
            <MainNavbar reserve />
            <h1>Reserve</h1>
        </div>
    );
}

export default Reserve;
